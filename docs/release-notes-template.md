# Murge {{VERSION}}

基于 mihomo 内核的 Windows 桌面客户端。

## 本次更新

- 修复升级后的内核启动恢复：应用更新后旧内核进程可能仍占用代理端口并被误判为新内核就绪；现在启动时会逐一核实混合/HTTP/SOCKS/控制器端口的进程归属并持续等待端口真正就绪，旧进程不再造成「显示运行中、实际流量未切换」的假成功。
- 内核启动失败即时呈现：特权内核若在控制器就绪前退出，会立即以服务端的具体原因报告失败，不再等待完整超时。
- 特权服务自动清理已退出进程的归属状态，避免残留状态阻塞下一次启动，并限制内核异常输出的采集体积。
