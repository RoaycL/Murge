# Murge {{VERSION}}

基于 mihomo 内核的 Windows 桌面客户端。

## 本次更新

- 升级安装保留特权服务与其受保护数据：应用内升级不再移除并重建 TUN 服务，geodata 与 provider 缓存原样保留，升级后的首次启动可离线完成、更快就绪；仅真实卸载才会移除服务与数据。
- 内置 GeoLite2-ASN 数据库（ASN.mmdb）：连接与规则可直接按自治域号分流，无需首次使用时再下载。
- 冷启动等待更充分：特权服务首次初始化 provider 缓存或首次下载所选滚动内核时，只要内核进程存活就持续等待，不再出现过早超时。
