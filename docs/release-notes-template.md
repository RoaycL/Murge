# Murge {{VERSION}}

基于 mihomo 内核的 Windows 桌面客户端。

## 本次更新

- 修复更新后必须先启动其他代理才能联网的问题：即使旧版卸载器已经清空 provider 缓存，Murge 也能先启动内核与本地代理，再自动刷新真实节点和规则数据，不再形成启动死循环。
- 冷启动兼容 YAML、TEXT 与 MRS 格式的代理和规则 provider；已有真实缓存始终保留，不会被临时启动数据覆盖。
- 保留内核启动失败的真实退出原因，后续状态轮询不再把关键诊断信息覆盖成笼统的“控制器未就绪”。
