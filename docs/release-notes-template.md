# Murge {{VERSION}}

基于 mihomo 内核的 Windows 桌面客户端。

## 本次更新

- 代理端口的启动前回收移入特权服务：升级后若旧版内核或提权状态的残留进程仍占用混合/HTTP/SOCKS/控制器端口，现在由 SYSTEM 服务在创建新进程前精确回收占用者——普通权限的图形端无法终止提权残留进程，这个场景不会再导致启动失败。
- 端口回收采用有界重试，超限后报告具体被阻塞的端口与占用进程；启动失败信息携带真实原因，不再只有笼统的「控制器未就绪」。
