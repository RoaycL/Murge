{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "Least-privilege capabilities for the main application window. The renderer talks to the shell exclusively through the desktop bridge command and Tauri events; no filesystem, shell, opener or broad core permissions are granted to the webview.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
